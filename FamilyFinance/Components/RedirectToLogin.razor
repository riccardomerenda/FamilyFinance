@inject NavigationManager Nav

@code {
    protected override void OnInitialized()
    {
        // Always redirect to login for protected pages
        Nav.NavigateTo("/Account/Login", forceLoad: true);
    }
}

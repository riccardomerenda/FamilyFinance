namespace FamilyFinance.Models;

public enum AccountCategory { Liquidity, Pension, Insurance }
public enum ReceivableStatus { Open, Received, Cancelled }
public enum GoalPriority { High, Medium, Low }
public enum GoalCategory { Liquidity, Investments, GrandTotal }

